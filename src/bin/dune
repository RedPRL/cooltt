(env (static (flags (:standard -ccopt "-static -s"))))

(executables
 (names main)
 (libraries cooltt.frontend cooltt.server cmdliner))

(install
 (section bin)
 (package cooltt)
 (files
  (main.exe as cooltt)))
