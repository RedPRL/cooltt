def + : nat → nat → nat = {
  \elim [
  | zero => \n => n
  | suc {_ => ih} => \n => suc {ih n}
  ]
}

def +-right-unit/cubical : (x : nat) (i : 𝕀) → sub nat {∂ i} [i=0 => {+ x 0} | i=1 => x] = {
  \elim [
  | zero => \i => 0
  | suc {x => ih} => \i => suc {ih i}
  ]
}
